# Lumen

A minimalist ambient music visualizer built with Next.js. It starts a muted YouTube lofi stream automatically (browser autoplay policies require a click to enable sound) and pairs it with a slow, breathing particle field.

## Run locally

```bash
npm install
npm run dev
```

Open http://localhost:3000.
